---
description: Bootstrap Content Brain — install deps, verify API keys, build the webapp.
---

You are setting up the **Content Brain** project for the user. Read the repo-root `CLAUDE.md` carefully and follow **Mode 1: First-time setup** exactly, in order, out loud (tell the user what you're doing at each step).

The user has either just cloned the repo or downloaded a zip. They might already have some prerequisites installed and `.env` configured — check before assuming you need to do everything from scratch.

Stop on any failure and surface the error to the user.

When you finish, end with the exact instruction from CLAUDE.md telling them how to start the server.
